import { ConvertStringToDate } from './convert-string-to-date';

describe('ConvertStringToDate', () => {
  it('should create an instance', () => {
    expect(new ConvertStringToDate()).toBeTruthy();
  });
});
