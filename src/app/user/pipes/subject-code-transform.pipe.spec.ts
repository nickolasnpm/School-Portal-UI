import { SubjectCodeTransformPipe } from './subject-code-transform.pipe';

describe('SubjectCodeTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new SubjectCodeTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
